# [INFRASTRUCTURE] F-00.1 — Setup Monorepo y Docker Compose

**Épica:** E-00 — Instalación y Configuración Base

## 🏷️ Categoría

**Categoría:** `Infrastructure`
**Impacta en:** Equipo Fullstack (Frontend + Backend)

## 📦 Dependencias

> **Punto de inicio** — no tiene dependencias previas. Debe completarse antes que cualquier otro backlog.

**Prioridad:** `Must`
**Sprint:** Sprint 1

---

**Descripción:**
Como desarrollador del equipo,
Quiero disponer de un monorepo estructurado con Docker Compose funcional,
Para poder ejecutar React + Laravel Sail + PostgreSQL en local con un único comando.

**Criterios de Aceptación (Checklist):**

- [ ] **Escenario 1 (Arranque local):**
  **Dado que** el desarrollador ha clonado el repositorio `Maya-AQSS/maya-dashboard`,
  **Cuando** ejecuta `docker compose up`,
  **Entonces** los servicios React (Vite dev server en puerto 5173), Laravel Sail (puerto 80) y PostgreSQL (puerto 5432) arrancan sin errores y están accesibles.

- [ ] **Escenario 2 (Estructura de monorepo):**
  **Dado que** el repositorio está inicializado,
  **Cuando** se inspecciona la raíz,
  **Entonces** existen al menos las carpetas `frontend/` (React + Vite) y `backend/` (Laravel), cada una con su propio `package.json` / `composer.json` y configuración independiente.

- [ ] **Escenario 3 (PostgreSQL compartido):**
  **Dado que** Docker Compose está corriendo,
  **Cuando** se conecta al contenedor PostgreSQL,
  **Entonces** existe una base de datos para Laravel y otra para Keycloak local, ambas accesibles desde sus respectivos servicios.

- [ ] **Escenario 4 (Variables de entorno):**
  **Dado que** existe un archivo `.env.example` en `backend/` y en `frontend/`,
  **Cuando** el desarrollador copia `.env.example` a `.env`,
  **Entonces** el sistema arranca sin requerir configuración adicional para el entorno local.

- [ ] **Requisito Técnico (Entorno de desarrollo unificado):**
  Para maximizar la experiencia de desarrollo (DevX), el archivo `docker-compose.yml` local levanta todo el stack necesario de forma autocontenida: React, Laravel Sail, PostgreSQL, RabbitMQ y Keycloak. En producción, estos servicios serán externos y se gestionarán exclusivamente vía variables `.env`.

---

**MoSCoW:** `Must`
**Sprint:** Sprint 1

**Notas:**

- El repositorio de referencia es `https://github.com/Maya-AQSS/maya-dashboard.git`. Actualmente es un repositorio con un proyecto React en la raíz. Las configuraciones e instalaciones del backend deben ser **el punto principal al inicio**.
- **Paso 1: Inicializar el backend.** En la raíz del repositorio y sin alterar el código actual, ejecutar `composer create-project laravel/laravel backend` para instalar **Laravel 13** desde cero.
- **Paso 2: Instalar Laravel Sail.** Dentro de la carpeta `backend/`, ejecutar `php artisan sail:install`, seleccionando únicamente PostgreSQL como servicio.
- **Paso 3: Validar el Backend.** Subir los contenedores (`./vendor/bin/sail up -d`) y asegurar que el ecosistema y la BD arrancan.
- **Paso 4: Configurar el monorepo (Frontend).** Una vez asegurado el backend, mover todo el código de React existente (incluyendo `package.json`, `src/`, `vite.config.js`, etc.) a una nueva carpeta llamada `frontend/`.
- El entorno utilizará por defecto Laravel Sail para los contenedores base (como PostgreSQL). Sin embargo, se deberá publicar y extender/editar el `docker-compose.yml` final en la raíz para orquestar también la imagen de **RabbitMQ** y la de **Keycloak local**.
- Los contenedores del `docker-compose` de desarrollo (`postgresql`, `rabbitmq`, `keycloak`, `laravel.test`...) compartirán la misma red interna.
- Los `.env` NO se commitean a git. Solo `.env.example` con valores de ejemplo para entorno local.
- CI/CD es responsabilidad del equipo de infra. Este backlog cubre solo el entorno de desarrollo local.
- El deploy final es en Kubernetes; este Docker Compose es exclusivamente para desarrollo local.
